# Keeps the Docker build context lean and prevents these from ever reaching
# a build stage. The runtime image only COPYs the compiled binary and
# migrations (see Dockerfile), so this is defense-in-depth rather than a fix
# for an actual leak — but it keeps `docker build` fast and makes the
# exclusion explicit instead of implicit.
perf/
.git/
tmp/
*.log
coverage.html
